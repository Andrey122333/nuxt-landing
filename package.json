{
  "name": "landing-nuxt-landing",
  "version": "2.0.0",
  "private": true,
  "description": "Landing page кафе 'Три Фреймворка' (Nuxt 3 SSR)",
  "type": "module",
  "scripts": {
    "dev": "nuxt dev --port 3000",
    "build": "nuxt build",
    "generate": "nuxt generate",
    "preview": "nuxt preview --port 3000",
    "start": "node .output/server/index.mjs",
    "postinstall": "nuxt prepare"
  },
  "dependencies": {
    "nuxt": "^4.4.5",
    "vue": "^3.5.34"
  },
  "devDependencies": {
    "@nuxtjs/google-fonts": "^3.2.0",
    "typescript": "^6.0.3"
  },
  "engines": {
    "node": ">=20.0.0"
  }
}
